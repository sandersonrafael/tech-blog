/* 'use client';

import UserConfirmationForm from '@/components/UserConfirmationForm';

const Unsubscribe = () => {
  return (
    <UserConfirmationForm formStyle="newsletter-unsubscribe" token="" />
  );
};

export default Unsubscribe;
 */
